import React from 'react'

export default function PartyTabsAddPCButton({ onClick }) {
  return (
        <div>
            <button id='addPC' onClick={onClick}>Add New Player</button>
        </div>
  );
}
